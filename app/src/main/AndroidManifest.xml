<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Linux.do is HTTPS-only; no storage, notification, media, or background-service permission is needed. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="dev.dimension.flare.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:description="@string/app_description"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlareDo"
        android:usesCleartextTraffic="false">
        <!-- Google Play services owns the QR camera surface, so FlareDo requests no camera permission. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <!--
          The launcher consumes no external payload. Authentication redirects use the dedicated,
          narrowly scoped component below instead of widening this filter.
        -->
        <activity
            android:name="dev.dimension.flare.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          This is the only exported data entry point. Runtime validation repeats this exact route,
          rejects ClipData, selectors, MIME types and URI grants, and rebuilds a defensive envelope
          that strips all extras without unparcelling or forwarding them (including nested intents).
        -->
        <activity
            android:name="dev.dimension.flare.auth.DiscourseAuthRedirectActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="auth_redirect"
                    android:scheme="discourse" />
            </intent-filter>
        </activity>
    </application>
</manifest>
