<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Linux.do is HTTPS-only; no storage, notification, media, or background-service permission is needed. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="dev.dimension.flare.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:description="@string/app_description"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlareDo"
        android:usesCleartextTraffic="false">
        <!--
          The launcher is the only app-owned exported component and consumes no external payload.
          Authentication redirects will use a dedicated, narrowly scoped component instead of widening
          this filter.
        -->
        <activity
            android:name="dev.dimension.flare.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
